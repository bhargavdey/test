from cgi import print_form


coordinates = (4, 5)#() is used to create tuples
#elements of tuples cannot be changed
print(coordinates[1])

coordinates2 = [(4, 5), (6, 7), (9, 10)]#tuples in a list
print(coordinates2[1])